// Generated by generate.py: importer 15.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_15(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_15(int n) {
    return compute_25(n + 0) + compute_28(n + 1) + compute_31(n + 2) + compute_34(n + 3) + compute_37(n + 4) + fast_3(helper_15(n));
}

std::string describe_15(int n) {
    return "use_15=" + std::to_string(use_15(n));
}
