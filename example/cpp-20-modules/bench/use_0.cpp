// Generated by generate.py: importer 0.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_0(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_0(int n) {
    return compute_0(n + 0) + compute_3(n + 1) + compute_6(n + 2) + compute_9(n + 3) + compute_12(n + 4) + fast_0(helper_0(n));
}

std::string describe_0(int n) {
    return "use_0=" + std::to_string(use_0(n));
}
