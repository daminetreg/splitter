// Generated by generate.py: importer 10.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_10(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_10(int n) {
    return compute_30(n + 0) + compute_33(n + 1) + compute_36(n + 2) + compute_39(n + 3) + compute_2(n + 4) + fast_2(helper_10(n));
}

std::string describe_10(int n) {
    return "use_10=" + std::to_string(use_10(n));
}
