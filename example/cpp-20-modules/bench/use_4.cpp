// Generated by generate.py: importer 4.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_4(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_4(int n) {
    return compute_28(n + 0) + compute_31(n + 1) + compute_34(n + 2) + compute_37(n + 3) + compute_0(n + 4) + fast_0(helper_4(n));
}

std::string describe_4(int n) {
    return "use_4=" + std::to_string(use_4(n));
}
