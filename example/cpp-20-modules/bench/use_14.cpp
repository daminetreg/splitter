// Generated by generate.py: importer 14.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_14(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_14(int n) {
    return compute_18(n + 0) + compute_21(n + 1) + compute_24(n + 2) + compute_27(n + 3) + compute_30(n + 4) + fast_2(helper_14(n));
}

std::string describe_14(int n) {
    return "use_14=" + std::to_string(use_14(n));
}
