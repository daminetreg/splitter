// Generated by generate.py: importer 18.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_18(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_18(int n) {
    return compute_6(n + 0) + compute_9(n + 1) + compute_12(n + 2) + compute_15(n + 3) + compute_18(n + 4) + fast_2(helper_18(n));
}

std::string describe_18(int n) {
    return "use_18=" + std::to_string(use_18(n));
}
