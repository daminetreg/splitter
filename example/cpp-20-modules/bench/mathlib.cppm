// Generated by generate.py: the interface unit the benchmark edits.
module;
#include <vector>
#include <string>
#include <map>
#include <algorithm>
#include <cstddef>
export module mathlib;

export inline int fast_0(int v) { return v * 2 + 1; }
export inline int fast_1(int v) { return v * 3 + 1; }
export inline int fast_2(int v) { return v * 4 + 1; }
export inline int fast_3(int v) { return v * 5 + 1; }

export int compute_0(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 0);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 1)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_1(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 1);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 2)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_2(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 2);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 3)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_3(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 3);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 4)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_4(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 4);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 5)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_5(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 5);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 6)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_6(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 6);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 7)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_7(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 7);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 8)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_8(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 8);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 9)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_9(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 9);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 10)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_10(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 10);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 11)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_11(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 11);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 12)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_12(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 12);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 13)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_13(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 13);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 14)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_14(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 14);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 15)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_15(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 15);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 16)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_16(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 16);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 17)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_17(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 17);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 18)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_18(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 18);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 19)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_19(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 19);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 20)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_20(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 20);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 21)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_21(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 21);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 22)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_22(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 22);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 23)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_23(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 23);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 24)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_24(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 24);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 25)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_25(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 25);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 26)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_26(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 26);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 27)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_27(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 27);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 28)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_28(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 28);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 29)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_29(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 29);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 30)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_30(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 30);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 31)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_31(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 31);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 32)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_32(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 32);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 33)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_33(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 33);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 34)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_34(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 34);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 35)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_35(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 35);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 36)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_36(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 36);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 37)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_37(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 37);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 38)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_38(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 38);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 39)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}

export int compute_39(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 97 + 3), 39);
    std::map<std::string, int> m;
    for (std::size_t k = 0; k < v.size(); ++k) m[std::to_string(k * 40)] += static_cast<int>(k);
    long acc = 0;
    for (const auto& [key, value] : m) acc += static_cast<long>(key.size()) * value;
    std::sort(v.begin(), v.end());
    return static_cast<int>(acc % 1000) + v.front() + n;
}
