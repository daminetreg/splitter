// Generated by generate.py: importer 1.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_1(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_1(int n) {
    return compute_7(n + 0) + compute_10(n + 1) + compute_13(n + 2) + compute_16(n + 3) + compute_19(n + 4) + fast_1(helper_1(n));
}

std::string describe_1(int n) {
    return "use_1=" + std::to_string(use_1(n));
}
