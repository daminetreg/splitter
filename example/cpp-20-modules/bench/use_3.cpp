// Generated by generate.py: importer 3.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_3(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_3(int n) {
    return compute_21(n + 0) + compute_24(n + 1) + compute_27(n + 2) + compute_30(n + 3) + compute_33(n + 4) + fast_3(helper_3(n));
}

std::string describe_3(int n) {
    return "use_3=" + std::to_string(use_3(n));
}
