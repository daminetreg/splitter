// Generated by generate.py: importer 26.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_26(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_26(int n) {
    return compute_22(n + 0) + compute_25(n + 1) + compute_28(n + 2) + compute_31(n + 3) + compute_34(n + 4) + fast_2(helper_26(n));
}

std::string describe_26(int n) {
    return "use_26=" + std::to_string(use_26(n));
}
