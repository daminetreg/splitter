// Generated by generate.py: importer 19.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_19(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_19(int n) {
    return compute_13(n + 0) + compute_16(n + 1) + compute_19(n + 2) + compute_22(n + 3) + compute_25(n + 4) + fast_3(helper_19(n));
}

std::string describe_19(int n) {
    return "use_19=" + std::to_string(use_19(n));
}
