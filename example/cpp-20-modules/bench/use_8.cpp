// Generated by generate.py: importer 8.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_8(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_8(int n) {
    return compute_16(n + 0) + compute_19(n + 1) + compute_22(n + 2) + compute_25(n + 3) + compute_28(n + 4) + fast_0(helper_8(n));
}

std::string describe_8(int n) {
    return "use_8=" + std::to_string(use_8(n));
}
