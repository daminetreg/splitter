// Generated by generate.py: importer 24.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_24(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_24(int n) {
    return compute_8(n + 0) + compute_11(n + 1) + compute_14(n + 2) + compute_17(n + 3) + compute_20(n + 4) + fast_0(helper_24(n));
}

std::string describe_24(int n) {
    return "use_24=" + std::to_string(use_24(n));
}
