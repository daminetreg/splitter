// Generated by generate.py: importer 16.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_16(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_16(int n) {
    return compute_32(n + 0) + compute_35(n + 1) + compute_38(n + 2) + compute_1(n + 3) + compute_4(n + 4) + fast_0(helper_16(n));
}

std::string describe_16(int n) {
    return "use_16=" + std::to_string(use_16(n));
}
