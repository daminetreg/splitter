// Generated by generate.py: importer 22.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_22(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_22(int n) {
    return compute_34(n + 0) + compute_37(n + 1) + compute_0(n + 2) + compute_3(n + 3) + compute_6(n + 4) + fast_2(helper_22(n));
}

std::string describe_22(int n) {
    return "use_22=" + std::to_string(use_22(n));
}
