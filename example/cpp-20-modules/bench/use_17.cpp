// Generated by generate.py: importer 17.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_17(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_17(int n) {
    return compute_39(n + 0) + compute_2(n + 1) + compute_5(n + 2) + compute_8(n + 3) + compute_11(n + 4) + fast_1(helper_17(n));
}

std::string describe_17(int n) {
    return "use_17=" + std::to_string(use_17(n));
}
