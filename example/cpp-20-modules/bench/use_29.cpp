// Generated by generate.py: importer 29.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_29(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_29(int n) {
    return compute_3(n + 0) + compute_6(n + 1) + compute_9(n + 2) + compute_12(n + 3) + compute_15(n + 4) + fast_1(helper_29(n));
}

std::string describe_29(int n) {
    return "use_29=" + std::to_string(use_29(n));
}
