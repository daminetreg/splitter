// Generated by generate.py: importer 20.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_20(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_20(int n) {
    return compute_20(n + 0) + compute_23(n + 1) + compute_26(n + 2) + compute_29(n + 3) + compute_32(n + 4) + fast_0(helper_20(n));
}

std::string describe_20(int n) {
    return "use_20=" + std::to_string(use_20(n));
}
