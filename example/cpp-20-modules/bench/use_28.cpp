// Generated by generate.py: importer 28.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_28(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_28(int n) {
    return compute_36(n + 0) + compute_39(n + 1) + compute_2(n + 2) + compute_5(n + 3) + compute_8(n + 4) + fast_0(helper_28(n));
}

std::string describe_28(int n) {
    return "use_28=" + std::to_string(use_28(n));
}
