// Generated by generate.py: importer 12.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_12(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_12(int n) {
    return compute_4(n + 0) + compute_7(n + 1) + compute_10(n + 2) + compute_13(n + 3) + compute_16(n + 4) + fast_0(helper_12(n));
}

std::string describe_12(int n) {
    return "use_12=" + std::to_string(use_12(n));
}
