// Generated by generate.py: importer 2.
import mathlib;
#include <vector>
#include <string>
#include <algorithm>
#include <numeric>

static int helper_2(int n) {
    std::vector<int> v(static_cast<std::size_t>(n % 13 + 1), n);
    std::iota(v.begin(), v.end(), n);
    return std::accumulate(v.begin(), v.end(), 0);
}

int use_2(int n) {
    return compute_14(n + 0) + compute_17(n + 1) + compute_20(n + 2) + compute_23(n + 3) + compute_26(n + 4) + fast_2(helper_2(n));
}

std::string describe_2(int n) {
    return "use_2=" + std::to_string(use_2(n));
}
